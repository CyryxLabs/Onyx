[pytest]
testpaths = tests
norecursedirs =
    .git
    .venv
    build
    dist
    release
    runtime
    docs
    .pytest*
    .tmp*
    .review*
    .phase*
    .codex-tmp*
addopts = --strict-config --strict-markers
