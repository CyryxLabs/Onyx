# Phase 7 Founder Command V1 — E6 acceptance

- Evidence ID: `VE-P7-FOUNDER-COMMAND-V1-E6-001`
- Decision date: `2026-07-23`
- Decision: **ACCEPTED — default-off cited executive projection**
- Candidate manifest:
  `b094e18b99342465e06cdc703b3a7db118ffe9fe3e3182171be61be06f63457e`
- Artifact root:
  `d636a5192224a48a0ef09038d0cfc4e805b16798c1857d7735b9f27748e0e633`

## Decision

Founder Command V1 is accepted as the next ordered Phase 7 slice. Findings
are `P0=0`, `P1=0`, `P2=0`, and `P3=0`.

The independent gate:

- rehashed all seven candidate artifacts and recomputed the artifact root;
- rehashed and interpreted all four accepted Approved Sources + Company Graph
  V1 entry roots;
- reproduced exact default-off return-before-entry behavior;
- executed Founder Command, graph, source, Domain Ledger, alias,
  control-plane, workspace and artifact suites in fresh Python processes;
- reproduced **168 passed tests, 65 passed subtests, 0 failed, 0 errors**;
- reproduced eight explained Windows skips for POSIX-only contracts;
- confirmed zero persistence, URL fetch, artifact-content read, model,
  network/process/provider/browser/action/live call or startup seam.

## Accepted behavior

The accepted generator:

- calls the accepted Company Graph projector itself and rehashes its complete
  output;
- produces daily Founder Brief and weekly operating-review cadences;
- separates known, inferred and unknown graph claims;
- supplies cited portfolio, blocker, dependency, proposed-decision and risk
  views;
- applies explicit per-semantic current, aging and stale deadlines;
- preserves declared contradictions and surfaces structured same-subject
  incompatible status conflicts without silent overwrite;
- accepts only graph-evidence-linked revenue opportunities and abstains rather
  than inventing revenue, customers, traction, runway or validation;
- ranks at most three recommended actions by impact, urgency, graph
  confidence, effort and downside;
- retains known context, inference, unknowns, alternative explanations,
  recommended action, verification method, completion evidence and downside;
- authenticates the prior brief and reports exact “what changed” claim and
  subject deltas;
- hashes every graph item, freshness result, conflict, portfolio view, queue,
  recommendation, delta, abstention and trust field in the returned brief.

## Limits retained

- V1 remains default-off and is not scheduled or wired to V13, startup, voice,
  UI or the dashboard.
- General open-text/NLP contradiction discovery is not implemented.
- Opportunity and action candidates require a later governed intake; the
  brief cannot execute a recommendation.
- Phase 7 is not exited and the full Onyx PRD is incomplete.

The next gate may aggregate the accepted Phase 7 slices and define the exact
read-only operational wiring without weakening source, workspace or authority
boundaries.
