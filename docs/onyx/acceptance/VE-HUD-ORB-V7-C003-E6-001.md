# Onyx HUD / Orb V7 Candidate 003 External E6 Acceptance

- Evidence ID: `VE-HUD-ORB-V7-C003-E6-001`
- Decision date: 2026-07-23
- Decision: **ACCEPTED**
- Findings: **P0=0, P1=0, P2=0, P3=0**

This decision accepts only HUD/Orb V7 Candidate 003 with candidate manifest
SHA-256
`3be0988e98b4f8222751686386041094606da4366c8111453b381d4cf47c84f2`
and independently recomputed ten-artifact root
`2047d2f60a64a7d47d1b568e6b24bf93faf9c1d5fa179372606e3c965ea6ba62`.

The external gate reproduced 19 candidate adversarials, nine independent E6
tests and 25 accepted V6 regression tests in separate Qt processes. It verified
the exact default-off flag, hash-authenticated V6 installation/host, drift-safe
rollback, QML load, all projection data and actions, reachable compact mode,
accessibility contracts, exactly one `QQuickWidget`, Cyryx tokens, absence of
third-party branding, and absence of QtQuick3D/WebGL.

The final 1440x900 Direct3D11 capture SHA-256 is
`ac05686b74829c59f404da455184e13bc073ce426ad882b4ddef8b12fe873399`.
It contains the dominant detailed neural sphere, natural halo and internal
mesh without external arc, ellipse, orbit or structural ring geometry. The
only curved line geometry outside the entity is the lower command keel. The
owner-approved composition was recaptured after making compact mode reachable;
that threshold change does not alter the 1440x900 composition.

Measured host CPU was 0.585% active, 0.096% idle and 0.042% hidden. Projection
targets were exactly 16/0/0 FPS; the visual timer is capped at 12 FPS.

## Resolved during the external gate

Before freeze, the gate removed a background `Canvas.arc`, decorative curves
outside the command keel and an internal structural border ring. It replaced
the mutable UI rollback dictionary with an immutable private restoration record
and authenticated the accepted V6 source, manifest and installed host. It also
raised the compact threshold above the real minimum window size so compact mode
is reachable and physically load-tested.

## Honest boundary

This E6 accepts a default-off Windows/Direct3D11 HUD package. It does not
activate V7, V10 or any shortcut. V9, V10 and launcher bytes were inspected and
left unchanged. Native macOS/Linux physical rendering, live activation and
cross-device performance remain outside this evidence.
