# Onyx Live Activation V10 C003 — Physical-Onboarding Fix Candidate

Status: **DEFAULT-OFF / UNWIRED / READY FOR EXTERNAL GATE**

V10 C003 preserves all C002 integrity fixes and accepted predecessor bytes. It composes exact evidence
for V9 E6, Phase 6 Live Wiring V1 E6, Phase 5 Exit Candidate V2 E6, and HUD /
Orb V7 C003 E6. It is not activated and performs no shortcut write.

## Fail-closed boundary

The exact active truth requires:

- complete canonical V9 active environment;
- `ONYX_LIVE_ACTIVATION_V10=1`;
- `ONYX_PHASE6_LIVE_WIRING_V1=true`;
- `ONYX_HUD_V7_LIVE=1`;
- hash-bound Python, bootstrap and launcher;
- exact accepted V9, Wiring V1, Phase5 C2 and HUD V7 evidence.

The HUD V7 closure binds exactly four ordered artifacts: the real
`core.onyx_hud_orb_v7` module, `hud-orb-v7-candidate` manifest, C003 E6 record
and C003 E6 metadata. No directory or future-acceptance discovery is allowed.
The in-memory module origin and exact install/uninstall code objects are also
authenticated so a module or callable spoof cannot bypass the disk hashes.
Prerequisite validation succeeds before host import; `--preflight-only` reaches
the host contract without installing V10, running `main.main()` or writing a
shortcut.

## Prepared composition

The frozen order is:

1. preflight flags, runtime and all four acceptance envelopes;
2. import `main`;
3. install/start exact V9;
4. obtain exact V7 from the V9 predecessor chain;
5. call exact `install_candidate(ui_module) -> bool` and require `True`;
6. create the canonical `runtime/phase6-live-wiring-v1` root;
7. construct/install accepted Wiring V1 against exact V7;
8. install V10 shortcut and setup persistence seams;
9. install a secure onboarding readiness seam before `MainWindow` construction;
10. install the configured-owner refresh seam;
11. expose the module controller before `main.main()`.

The planned 35 seams are V9's 27, one HUD V7 replacement, Wiring V1's three
lifecycle seams, and four V10 shortcut/setup/onboarding/configured-owner seams. V10 does not patch
`_execute_tool`, `_run_live_loop` or `_send_realtime`, and adds no user-facing
Phase 6 route.

## Rollback

Rollback restores exact installed V9: it restores the V9 setup, onboarding and shortcut
wrappers, closes all Wiring V1 sessions, restores its lifecycle seams,
uses its immutable private authority to call exact
`uninstall_candidate(ui_module) -> bool`, requires `True`, restores
the V9 predecessor surface and restores the exact V9 environment. macOS and
Linux delegate directly to V9.

## Evidence

- focused tests: 33 passed;
- strict flag/empty bootstrap truth: pass;
- exact four-artifact HUD V7 C003 E6 binding: pass;
- prerequisite validation before `main`/`ui`: pass;
- exact one-argument HUD install/rollback contract: pass;
- in-memory HUD module/function spoof denial: pass;
- all four HUD roots and all ten manifest leaves/QML tamper denial: pass;
- active/rollback CMD arbitrary-CWD normalization: pass;
- already-configured owner shortcut refresh contract: pass;
- secure credential plus valid OS and blank/placeholder owner reaches READY: pass;
- missing credential, missing/invalid OS and unreadable config retain setup: pass;
- exact literal `Sir` fallback and first-contact `set_owner_name` directive: pass;
- real Qt host physical smoke with intercepted shortcut writer: pass;
- truthy non-boolean install result refusal: pass;
- zero-network accepted preflight: pass;
- runtime manifest/hash/path binding: pass;
- Linux delegation to exact V9: pass;
- V9/Wiring V1/Phase5 C2 accepted envelope hashes: pass;
- Phase5 C2 historical 275/2 envelope retained without future discovery;
- Ruff, format and Python compilation: pass;
- physical activation, shortcut write and provider/network calls: none.

No V10 live activation, shortcut write or E6 acceptance is claimed by this
checkpoint. C003 is ready for an independent external gate.
