# Onyx V100 source successor — pre-seal evidence

This is a source-candidate record, not a GO, installation, hardware or provider
certificate. Product version remains 1.2.0; installed V96 / 1.1.31 is unchanged.
Humanoid geometry, palette, layout and voice selection are unchanged.

## Why a successor is necessary

The canonical V99 run reached 45,605 passed, 8 failed, 52 skipped and 229
subtests in 1,079.13 seconds (onyx-v99-canonical-final.xml in the workspace
parent). The remaining-file diagnostic reached 5,478 passed, 31 failed,
68 skipped and 477 subtests. These overlapping diagnostic totals must not be
added or presented as a successful full run.

Observed failures were stale HUD/package/source authority and three active
test assumptions: renderer selector 16 instead of 17; fixed version 1.1.10
instead of current source version; globally-current V56 lint authentication.
The tests now retain the actual invariants, not the obsolete positive result.

Before any V100 edits, all 669 V99 closure entries plus its verifier and fixture
were copied to ../onyx-v99-source-evidence-20260905.zip (workspace parent).
All 671 compressed entries were independently read and hash-checked.
ZIP SHA256: 7ccec7100213d96af76c206d6af3437a63a8ba464953cb368a576ad255d6a455.
V99 fixture SHA256 remains ac6ae93d21f03707460e8cc566df36376005a02d7ea29dc91d71d8401fd89f83.
This archive is source evidence, NOT an owner-data recovery backup.

## Current changes and coverage

- Registry V2 carries all 39 old exact routes, adds 24 observed HUD/package
  terminal nodes, one V62 positive and 58 V99 post-source nodes: 122 total.
  Its 148 bindings retain original V1 records and historical bytes.
  No wildcard, new collection exclusion, historical fixture repin or skipped
  negative test is introduced.
- Every routed invocation authenticates real V100, HUD48 and package17 before
  consulting a process-local successor-suite cache.
- The old V33 linked-broker negative requires actual current HUD link tests AND
  V100's exact linked permission-broker rejection. A stale version failure
  cannot count as link protection.
- 30 physical link tests and 24 current humanoid smoke-target tests passed,
  without skips. The latter are AST/QML tests, not a physical GPU/FPS result.
- The actual stager is checked for all 26 capability runtime files, exact port
  membership and three required historical receipt files. Tamper, omission,
  added-member and physical same-byte-link tests run after a valid baseline.
  Eleven preparation tests passed; full source admission requires the real seal.
- Build entry now authenticates V100. Staged capability validation runs after
  HUD17 validation and before package hygiene acceptance.
- The source/HUD succession, link/smoke and stage preparation ring passed 110
  tests. Its one deliberately premature source-admission test rejected the
  unsealed V100. Full source/test qualification remains pending at this record.
- Ruff passed across actions, core, memory, scripts, tests, main.py and ui.py.
  npm lint/typecheck/test are unavailable because this Python snapshot has no
  package.json; they are not reported as passes.

## Remaining gates — no implied completion

1. Seal and independently qualify V100, then repeat canonical full regression.
2. Quiesce Onyx cooperatively and produce a private, identity/hash-verified
   native owner-data backup with an independent restore check before upgrade.
   No owner data has been excluded, deleted, copied or exported in this record.
3. Build/qualify/install a local unsigned successor with rollback. V96 remains
   installed; source fixes do not yet change the user's running application.
4. Physical AB13X/JOUNIVO voice, interruption/reconnection, camera/hand,
   desktop/mobile rendering and latency/FPS still require live evidence.
5. Owner OAuth, social account/target activation, real uploads and Telegram
   delivery require exact consent and provider evidence. FFmpeg/ffprobe 7.1.1
   were observed installed; no social-video workflow is certified from that.
6. Phone Link is paired to Android, but actual governed call execution, duplex
   audio, recovery, inbound isolation and iPhone acceptance remain incomplete.
7. Docker live plugin execution, installed mission/squad/business workflows,
   optional vault-to-context activation and Guardian operational acceptance
   remain separate gates. Local foundation tests are not operational parity.
8. Public distribution requires its own signing and release approval.

Native-data preflight found 3,676 reparse entries confined to historical test
trees. The 32 initially inaccessible paths were Windows long-path lookup errors,
not access-denied errors; extended-path metadata succeeded. No blanket test-root
exclusion or claim of recoverability follows from this diagnostic.

Post-seal results must be written as new external evidence, not by changing
this sealed pre-seal record.
